# Agent instructions

Read [`CLAUDE.md`](CLAUDE.md). Rules and routing live there.

This file exists only so Codex-based and other non-Claude agents find that
pointer; it is kept under 20 lines on purpose so it never needs its own rules.
