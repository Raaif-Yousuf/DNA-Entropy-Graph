# ToTest queue

Hard Rule 17's one carve-out from "GitHub Issues is the only tracker": a
closed issue whose behaviour could not be proven on the thing it actually
ships on. Not a backlog — a queue that must drain. See
[`docs/README.md`](README.md)'s house-style table for how this surface
relates to Issues and `sprint_log.md`.

## Rules

1. Add a row when you close an issue whose behaviour you could not prove on
   the thing it ships on: an installer build, a real GPU VM, a second
   Google account, a local GPU. **"Tests pass" is never that proof** — see
   the `wired-to-nothing` skill for why a green suite does not prove a
   feature is reachable.
2. A row names: the issue, the closing commit sha (validated against the
   real commit graph, not typed from memory), what a human needs (`Needs`),
   the exact action to take, what passing looks like, and what a **false**
   pass looks like — the thing that would fool a quick check into reporting
   success when the behaviour is actually still broken.
3. Delete the row when someone verifies it. If verification fails, reopen
   the issue with the measured evidence, then delete the row — a failed
   verification does not stay in this table, it goes back to being an open
   issue.
4. Group rows by `Needs`, and drain by group, because the cost here is
   setup (booting an installer build, spinning up a GPU VM), not the
   individual check. Doing five `installer`-needs rows in one sitting costs
   one installer build, not five.
5. [`docs/release_runbook.md`](release_runbook.md) drains every `installer`
   row as part of every release. A release with `installer` rows still open
   is not a release.
6. `scripts/check_totest_format.py --max-age-days 45` (once that script
   exists) fails CI on any row older than 45 days, so this table cannot
   quietly turn into a second backlog.

`Needs` is one of: `app-dev`, `installer`, `gpu-vm`, `cpu-vm`,
`two-accounts`, `local-gpu`.

## Row format

```
| # | Closed by (commit sha) | Needs | Do this | Passing looks like | False pass looks like |
```

## Queue

Three rows, all `Needs: cpu-vm` and all from the same closing commit, so they
drain in one sitting per Rule 4: the worker subpackage (#278) implements
every cloud-facing path (the Compute API lifecycle calls, the real GCS REST
path, and the app/worker version-mismatch refusal #250 also asks for) and
tests every one of them against `LocalBlobstore` and a scripted fake, per
its own closing comment, because nothing tonight was allowed to spend
money. None of it has ever touched a real Google Cloud project.

| # | Closed by (commit sha) | Needs | Do this | Passing looks like | False pass looks like |
| --- | --- | --- | --- | --- | --- |
| #278 | dcd89a7 | cpu-vm | On a real Compute Engine VM (any machine type, no GPU needed) with the worker service account attached, run a real job through to a terminal status with `lifecycle.afterTask=stop`, then again with `afterTask=delete`, and watch the VM's real state (Console or `instances.aggregatedList`) rather than only the worker's own exit code. | The VM reaches `TERMINATED` (stop) or disappears (delete) within seconds of the worker's terminal status, and the Compute operation history shows the stop/delete request came from the worker's service account via the metadata token, well before `maxRunDuration` could have expired. | The VM eventually reaches `TERMINATED` or is deleted, but only because `scheduling.maxRunDuration` expired and `instanceTerminationAction=DELETE` fired, indistinguishable from a real pass by looking at the VM's final state alone; only the operation's actor and timing tell them apart. |
| #278 | dcd89a7 | cpu-vm | From that same VM, run the CPU smoke test (mock predictor, `-cpu` image) against a real `gs://` bucket so `GcsBlobstore` writes `status.json`/`progress.jsonl`/`result.json`/outputs through the real metadata-token REST path, then read every one of them back. | Every object the worker claims to have written is a real, readable object in the bucket (`gsutil ls`/Console), the bytes round-trip exactly, and the app (or `gsutil cp`) can download `result.json` and every output afterward. | `pytest -m "not gpu"` stays green (309 passed, 2 skipped), it already is, and proves nothing here, since every one of those tests runs against the in-memory fake `Blobstore`, which does not exercise the real REST calls, auth header, retries, or GCS's own generation semantics. |
| #278 | dcd89a7 | cpu-vm | Build or point the app's dev override at a worker image whose supported schema disagrees with a real app build's `manifest.json.schema`, run one real job through the actual container on a real VM, and watch what the app shows (#250's scope; #278 shipped the worker-side half). | The job fails within seconds of the container starting; `status.json.error.code` is `WORKER_VERSION_MISMATCH` naming both the manifest's declared schema and the worker's supported version; the app (once `app/`, issue #61, exists) shows exactly one action. | `worker/tests/test_worker_manifest.py` and `test_worker_runner.py`'s in-process schema-mismatch tests pass (they already do), they prove the Python refusal logic is correct, not that a real container boot-and-refuse cycle reads as a clean, one-action failure to a user rather than a hang or a raw stack trace, since `app/` does not exist yet to render that action at all. |
| #318 | a9d884b | cpu-vm | Also covers #319. From a real Compute Engine VM (no GPU needed) with the worker service account attached, run a real job through `GcsBlobstore` against a real bucket and, while it is running, cause a real transient failure on the store (revoke/expire the token mid-run, or throttle the connection into a real `429`/`500`) rather than a scripted fake. | `status.json`'s `heartbeatSeq` keeps increasing across the injected failure and afterward (the background thread survives it); `StatusWriter.consecutive_write_failures` rises during the outage and returns to `0` once the store is reachable again; the input that happened to be mid-contig when the failure hit is **not** marked `failed`/`INPUT_INVALID` purely because a status write bounced; a `status writer: ... write failed` line appears on the worker's own stderr/container logs each time. | `worker/tests/test_worker_status.py`'s injected-fake-transport tests stay green (they already do, 449 passed/1 skipped at the time this landed) — they prove `_safe_write` catches a `BlobstoreError` the test itself raises on cue, not that a real GCS `401`/`429`/`500`/truncated read from the actual REST path, a real background thread under real system load, or a real metadata-token expiry produces exactly that same exception shape. |
| #321 | a9d884b | cpu-vm | From that same VM, call `apply_lifecycle("stop")` then, on a second disposable VM, `apply_lifecycle("delete")` for real against the Compute API (or exercise the equivalent path through a real job's `lifecycle.afterTask`), and watch both the returned operation body and the VM's actual state (Console or `instances.aggregatedList`), not just whether the Python call raised. | The Compute API accepts the call; the operation id `runner.py` logs via `status.notice` matches a real operation in the project's operation history; the VM actually reaches the corresponding state (`STOPPED` / gone) rather than only being reported as accepted. | `worker/tests/test_worker_lifecycle.py`'s fake-opener tests stay green — they assert against a hand-built response body the test itself constructs, which proves the parsing logic works on a shape someone imagined, not that it matches what the real Compute API returns for `stop`/`delete` on a real instance, or that a real accepted-then-later-failed operation (the exact scenario #321 exists to catch) is even distinguishable from a normal success in practice. |
| #317 | 2061d38 | gpu-vm | Once `CloudCli` (#60) and `app/` (#61) exist, run `scripts/cloud_gpu_test.ps1 -Apply` for real (today it fails immediately and cleanly with a "CloudCli not found" message naming #60/#61/#24 — that failure is itself correct, not a sign this row is done) and watch the actual GCP Console / `instances.aggregatedList`, not the script's own printed plan. | A real, correctly labelled GPU VM (the full label set from `docs/job_contract.md` section 2) is created, runs to completion or its `maxRunDuration` timeout, and is torn down cleanly afterward with nothing left running — the script's own "nothing left running" interrupt-safety contract holds under a real Ctrl-C too. | The dry-run plan (no `-Apply`) prints a plausible machine type, zone, labels, and cost estimate and reads as if something were verified — dry-run creates nothing, by design, and a clean "CloudCli not found" failure from `-Apply` right now is the CORRECT behaviour, not evidence anything downstream of it has ever actually run. |
| #37 | bdca064 | gpu-vm | Also covers #36. On a real GPU VM: `docker build -f worker/Dockerfile.cuda -t dna-entropy-worker:cuda worker`, then `docker run --gpus all dna-entropy-worker:cuda dna-entropy-worker selftest`, then a real job with `predictor.kind: evo` against a real Evo 2 model — issue #37's own scope ("NGC PyTorch base + pip install evo2 imports cleanly and reproduces prototype numbers"), flagged directly in the Dockerfile's own header as this row's source. | The image builds without error; `evo2`/`flash-attn`/Transformer Engine import cleanly inside the container; `selftest` prints `OK`; a real Evo 2 prediction reproduces the prototype's recorded numbers within the tolerance `science_and_formats.md` section 3 documents. | The file exists, is well-commented, and reads as plausible — none of which confirms the pinned `nvcr.io/nvidia/pytorch` tag is a real, pullable image, that `evo2` installs cleanly on top of it, or that its CUDA/driver combination is even compatible with any real GPU tier this project targets. This session's machine has no CUDA stack and no reachable Docker daemon, so `docker build` has never once been run against this file. |
| #61 | a8dd31a | app-dev | Launch the built app: `app/src/DnaEntropyGraph.App/bin/x64/Debug/net10.0-windows10.0.19041.0/win-x64/DnaEntropyGraph.App.exe`. Watch for a window, and read the text in it. | A window appears, and every string in it is real English text from `Strings/en-US/Resources.resw`. | The build is green and the tests pass, which they already do and which prove nothing here: an unpackaged WinUI 3 app resolves the Windows App SDK **bootstrapper at runtime**, not at build time, so a bootstrapper that cannot resolve produces an exe that exits silently with no window and no error. Worse, a `.resw` that is not actually packed as a PRI resource shows a window whose every label is **blank**, which reads as an unfinished layout rather than as a broken build. |
| #61 | a8dd31a | app-dev | From `app/` as the working directory, run `dotnet test` on each of the five test projects; then try the same command from the repo root. | All five pass from `app/`. | Running from the repo root fails with "Testing with VSTest target is no longer supported", and reads like a broken test project rather than a working-directory problem: `global.json`'s `test.runner` setting is resolved from the **current working directory**, not from the project path. `ci-app.yml` already sets `working-directory: app` on every step, so CI would not have found this; the trap is for a human or an agent running the command by hand from the repo root, which is where it reads as a broken test project. |
| #341 | ee419a3 | cpu-vm | On a real Compute Engine VM, run a job whose store write fails persistently (revoke the worker service account's bucket access mid-run), let the run finish, then inspect the VM's own disk at `%TEMP%/dna-entropy-fallback` (`/tmp/dna-entropy-fallback` on the Linux image) before the VM is deleted. | An `ESCALATED` line appears on the serial console naming the job and the failure count, and a `<jobId>-status.json` and, if the final write also failed, a `<jobId>-result.json` are really on that disk with the run's true outcome in them. | The unit tests pass, which they already do: they monkeypatch `LOCAL_FALLBACK_DIR` to a `tmp_path`, so they prove the code writes to the directory it is told to, not that the real default directory is writable by the worker's user inside the container, nor that anything survives long enough to be read. A VM that reaches `instanceTerminationAction=DELETE` loses the whole disk, so a fallback that was written perfectly and then deleted looks exactly like one that was never written. |
| #57 | 47d5cfb | gpu-vm | Trigger a real `compute.instances.insert` failure on a project with billing unlinked, and again on one with the Compute API disabled, and capture the actual `Operation.Error` / `GoogleApiException` payload. Feed each through `CloudErrorClassifier.Classify`. | Each classifies to `Billing` / `ApiDisabled` from the payload's real structured `Code` and HTTP status, not only from a message substring, and the abort-vs-continue branch that follows matches `docs/cloud_design.md` section 5's table. | The fixture-driven tests stay green (72/72 in Cloud.Tests, they already are). They replay `tests/contract-fixtures/cloud_error_classification.json`, which is **stderr text only**, so they prove the substring fallback and its evaluation order are right and say nothing about the structured branch, which is modelled from a table nobody has checked against a real payload. Worse: billing off and Compute API off both read as a capacity problem in the message text, so a classifier that quietly falls through to `Stockout` sends the user hunting for a GPU while their billing account is simply unlinked. |
| #49 | 47d5cfb | gpu-vm | Provoke or wait for a real zonal stockout on a GPU tier, and a real regional quota refusal, and compare each real error's fields against what `FakeGcp.WithZoneStockout` and `WithQuotaExceededOnCreate` throw. | The real error carries the same `CloudErrorKind` and a structurally comparable `CloudError.Code`, so a test written against the fake would have caught the real failure. | Every scripted-failure test passes, which proves only that the fake agrees with itself. `FakeGcp` and `CloudErrorClassifier` were written by the same agent in the same session against the same document, so a failure shape that document gets wrong is wrong in both, and no test between them can see it. Quota is per-region and fixable by the user; stockout is per-zone and transient and the only one worth retrying, so conflating them wastes either the user's afternoon or the zone ladder. |
| #55 | 47d5cfb | gpu-vm | Send a real `instances.insert` carrying each of the six standard labels at the edge of what `VmSpec` now permits (63 characters, leading digit, trailing hyphen) and a `VmName` at 63 characters, and see which the API actually accepts. | Compute Engine accepts every spec `EnsurePreconditions()` passes, and rejects nothing that it passed: the guard is not merely stricter than the API but correctly aligned with it. | `VmSpecTests` passes, which proves the guard rejects what the guard's author believed the API rejects. The charset `^[a-z0-9_-]{1,63}$` was read off documentation, not measured. A guard **stricter** than the API silently refuses a legal spec and looks like correctness; a guard **looser** than the API lets an unlabelled or misnamed resource through, and an unlabelled resource is invisible to the Cloud page and therefore a leak that bills until someone notices. |
